[tool.poetry]
name = "jungle"
version = "0.1.0"
description = "The basal dev environment"
authors = ["karthikskumar <karthikskumar@github.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
